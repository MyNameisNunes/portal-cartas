Frontend (HTML/CSS/JS puro)

Abra `frontend/index.html` no navegador ou sirva via um servidor estático.

Estrutura:
- `css/styles.css` - estilos
- `js/app.js` - scripts
- `assets/` - imagens e uploads
