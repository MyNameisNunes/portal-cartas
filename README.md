# Portal de Gestão de Cartas

Estrutura inicial do projeto para o Desafio Técnico.

Subpastas:
- `backend/` - Código PHP sem frameworks (APIs e lógica)
- `frontend/` - HTML/CSS/JS vanilla

Credenciais de teste (exemplo):
- usuário: admin@example.com
- senha: password123

Decisões de UX/Produto (resumo curto):
1. Formularios claros e mínimo de campos para facilitar usuários pouco técnicos.
2. Feedback visual (loading) ao buscar edições por jogo para transparência no carregamento.

Veja as pastas `backend` e `frontend` para arquivos esqueleto.
